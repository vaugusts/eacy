# OpenAI Integration

This directory stores repo-native contracts and guidance for OpenAI provider
usage.

For feature `002-telegram-note-input-drive-landing`, the transcription
contract should define request/response shape, model configuration inputs, and
error semantics consumed by `apps.workers.transcription_adapter`.
